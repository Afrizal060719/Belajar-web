export function sendCoffee(name, callback) {
    const estimationTime = 2000;
    let isSucess = false;

    consol.log('Pramusaji sedang mengantarkan pesanan');

    setTimeout(() => {
        const number =
    })
}