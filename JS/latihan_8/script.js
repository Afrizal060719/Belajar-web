let btn = document.getElementById('btnHitung');
let tsd = document.getElementById('totalSebelum');
let diskon = document.getElementById('diskon');
let tb = document.getElementById('totalBayar');

btn.addEventListener('click', function() {
    let inputP = document.getElementById('jamParkir').value;
    let nparkir = Number(inputP);
    
    if (nparkir < 2) {
        total = nparkir * 5000;
    } else {
        let 
    }
})